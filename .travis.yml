sudo: required

language: c

services:
  - docker

before_install:
  - docker info
  - docker build --no-cache -t izonder/anny .

script:
  - docker run -it --rm izonder/anny node -v
  - docker run -it --rm izonder/anny yarn --version

after_success:
  - >
    curl -X POST -H 'Content-Type: application/json' --data '{"source_type": "Branch", "source_name": "nodejs-6"}' https://registry.hub.docker.com/u/izonder/anny/trigger/$DOCKER_TRIGGER_TOKEN/
